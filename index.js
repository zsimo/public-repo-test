"use strict";

/**
 * this repo needs to import a dependency
 */
var uniqueRandomArray = require('unique-random-array');


module.exports = {
    init: function () {
        console.log("ok");
    }
};